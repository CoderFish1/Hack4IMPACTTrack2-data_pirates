import LoginPage from "../login/page";

// For hackathon purposes, the register page is functionally identical to the login page (Instant Auth Bypass).
export default function RegisterPage() {
  return <LoginPage />;
}

